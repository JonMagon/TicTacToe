#pragma once

const int DEFAULT_FIELD_SIZE = 3;	// Размер игрового поля в квадратах
const int FIELD_SIZE_PX = 500;		// Размер игрового поля в пикселях
const int CELL_SIZE = 120;
const int LINE_WIDTH = 2; // Толщина линий игрового поля

const int INDENT_FIELD_X = 50; // Отступ перед игровым полем по X в пикселях
const int INDENT_FIELD_Y = 50; // Отступ перед игровым полем по X в пикселях

const std::string FONT_PATH = "fonts/calibri.ttf";
const int DEFAULT_FONTSIZE = 17;

const int BUTTON_DEFAULT_WIDTH = 100;
const int BUTTON_DEFAULT_HEIGHT = 30;
